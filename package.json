{
  "name": "soseedyjs",
  "version": "1.0.0",
  "description": "SoSeedy in Node.js",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf build && babel src -d build && cp -r src/html build/html",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build && node index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.3",
    "js-yaml": "^3.9.0",
    "path": "^0.12.7",
    "request": "^2.81.0"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-latest": "^6.24.1"
  }
}
